/** Archive published jobs older than this many days (strips heavy SEO fields). */
export const JOB_ARCHIVE_AFTER_DAYS = 90;

/** Permanently delete archived jobs older than this many days. */
export const JOB_PURGE_ARCHIVED_AFTER_DAYS = 180;
