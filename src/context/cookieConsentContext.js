import { createContext } from 'react';

/** @type {import('react').Context<import('./CookieConsentContext.jsx').CookieConsentContextValue | null>} */
export const CookieConsentContext = createContext(null);
